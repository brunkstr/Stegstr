{
  "name": "stegstr-relay-proxy",
  "version": "1.0.0",
  "description": "Nostr WebSocket proxy: forwards client traffic to configurable backend relays.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
